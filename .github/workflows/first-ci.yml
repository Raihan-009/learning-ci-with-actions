name: Basic Checkout Lab

on:
  push:
    branches:
      - main

jobs:
  docker-version:
    runs-on: ubuntu-latest
    steps:
      - name: List files
        run: ls -la
        
      - name: Show system info
        run: docker --version
